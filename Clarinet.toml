[project]
name = 'On-Chain-Lost-and-Found-Item-Registry'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.On-Chain-Lost-and-Found-Item-Registry]
path = 'contracts/On-Chain-Lost-and-Found-Item-Registry.clar'
clarity_version = 3
epoch = 3.1

[contracts.smart-matching]
path = 'contracts/smart-matching.clar'
clarity_version = 3
epoch = 3.1
[contracts.witness-system]
path = 'contracts/witness-system.clar'
clarity_version = 3
epoch = 3.1
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
